
name: Build SDK (iOS)

on:
  workflow_call:
  workflow_dispatch:

jobs:
  build-ios:
    runs-on: [ self-hosted, macOS, ARM64, m1-1317 ]
    steps:
    - uses: actions/checkout@v2
    - name: git-update-dependecies
      run: git submodule update --init --recursive
    
    - name: Find NDK SHA
      run: |
        INWORLD_NDK_SHA=$(git submodule status | cut -d " " -f 2)
        echo $INWORLD_NDK_SHA
        echo "INWORLD_NDK_SHA=$INWORLD_NDK_SHA" >> $GITHUB_ENV
    
    - uses: actions/cache/restore@v3
      id: ndk-cache-ios
      with:
        path: InworldAI/inworld-ndk/build/Package
        key: ${{ runner.OS }}-ndk-cache-iOS-${{ env.INWORLD_NDK_SHA }}
        fail-on-cache-miss: true
        enableCrossOsArchive: true
    - name: check-cache-success
      if: steps.ndk-cache-ios.outputs.cache-hit != 'true'
      run: |
        echo "cache restore failed"
        exit 1
    
    - name: Copy NDK
      run: |
        python3 "build-scripts/ndk-util.py" --copy
    - run: mkdir -p TestInworldFPS 
    - uses: actions/checkout@v2
      with:
        ref: 'TestInworldFPS'
        path: 'TestInworldFPS'
    - name: Copy Plugins & Remove Assets
      shell: bash
      run: |
          rm -rf "InworldAI/Content"
          rm -rf "TestInworldFPS/TestInworldFPS/Plugins/InworldAI"
          rm -rf "TestInworldFPS/TestInworldFPS/Plugins/InworldMetahuman"
          rm -rf "TestInworldFPS/TestInworldFPS/Plugins/InworldInnequin"
          mkdir "TestInworldFPS/TestInworldFPS/Plugins"
          cp -r "InworldAI" "TestInworldFPS/TestInworldFPS/Plugins"
          cp -r "InworldMetahuman" "TestInworldFPS/TestInworldFPS/Plugins"
          cp -r "InworldInnequin" "TestInworldFPS/TestInworldFPS/Plugins"
    
    - name: BuildGame-ios-51
      shell: bash
      run: |
          sh "/Users/Shared/Epic Games/UE_5.1/Engine/Build/BatchFiles/RunUAT.sh" > BuildGameLog.txt BuildGame -project="$PWD/TestInworldFPS/TestInworldFPS/TestInworldFPS.uproject" -platform=IOS  -notools -configuration=Development+Shipping
          cat BuildGameLog.txt
          if (grep -Fxq "BUILD SUCCESSFUL" BuildGameLog.txt)
          then
              exit 0
          fi
          exit 1
    - name: BuildPlugin-ios-51
      shell: bash
      run: |
        sh "/Users/Shared/Epic Games/UE_5.1/Engine/Build/BatchFiles/RunUAT.sh" > BuildPluginLog.txt BuildPlugin -plugin="$PWD/TestInworldFPS/TestInworldFPS/Plugins/InworldAI/InworldAI.uplugin" -TargetPlatforms=IOS -package="$PWD/PluginBuild"
        cat BuildPluginLog.txt
          if (grep -Fxq "BUILD SUCCESSFUL" BuildPluginLog.txt)
          then
              exit 0
          fi
          exit 1

         
